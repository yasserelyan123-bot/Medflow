import {useEffect,useState} from 'react';import {api} from '../api/client';
export default function Appointments(){const[list,setList]=useState<any[]>([]);useEffect(()=>{api<any[]>('/appointments').then(setList).catch(()=>setList([]))},[]);return <><div className="page-head"><div><h1>المواعيد</h1><p>عرض المواعيد القادمة وحالاتها.</p></div></div><div className="table-wrap"><table><thead><tr><th>المريض</th><th>الطبيب</th><th>التاريخ</th><th>الحالة</th><th>النوع</th></tr></thead><tbody>{list.map(a=><tr key={a.id}><td>#{a.patient_id}</td><td>#{a.doctor_id}</td><td>{new Date(a.start_time).toLocaleString('ar-EG')}</td><td><span className="badge">{a.status}</span></td><td>{a.type}</td></tr>)}{!list.length&&<tr><td colSpan={5}>لا توجد مواعيد</td></tr>}</tbody></table></div></>}
