const API=import.meta.env.VITE_API_URL || 'http://localhost:8000/api/v1';
export function token(){return localStorage.getItem('medflow_token')}
export async function api<T>(path:string,options:RequestInit={}){const headers=new Headers(options.headers); if(options.body && !(options.body instanceof FormData) && !headers.has('Content-Type'))headers.set('Content-Type','application/json'); const t=token(); if(t)headers.set('Authorization',`Bearer ${t}`); const r=await fetch(`${API}${path}`,{...options,headers}); if(r.status===401){localStorage.removeItem('medflow_token');window.dispatchEvent(new Event('medflow-logout'));} if(!r.ok){let msg='حدث خطأ في الطلب';try{const e=await r.json();msg=e.detail||msg}catch{} throw new Error(msg)} return r.status===204?null as T:r.json() as Promise<T>}
export async function login(username:string,password:string){const body=new URLSearchParams({username,password});return api<{access_token:string;token_type:string}>('/auth/login',{method:'POST',body})}
